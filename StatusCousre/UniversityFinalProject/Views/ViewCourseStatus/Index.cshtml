@model IEnumerable<UniversityFinalProject.Models.ViewCourseStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CourseAssignDepartment.Code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CourseAssignDepartment.Code)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ViewCourseStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ViewCourseStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ViewCourseStatusId })
        </td>
    </tr>
}

</table>
